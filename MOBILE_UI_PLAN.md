# خطة واجهة الجوال والتابلت — NT Commerce (p275)

> **الحالة: وثيقة للموافقة — لم يُنفَّذ أي تغيير بعد.**
> القاعدة الحاكمة: سطح المكتب لا يُمسّ إطلاقاً؛ كل التعديلات مشروطة بنقاط كسر (`md:` / `lg:`) أو مكوّنات بديلة تظهر على الشاشات الصغيرة فقط.
> التاريخ: 2026-08-23

---

## 1. الوضع الحالي (جرد ميداني)

| العنصر | الحالة |
|---|---|
| الهيكل العام (Layout) | ✅ جاهز: ترويسة جوال + زر هامبرغر + درج جانبي منزلق + زر رجوع (`md:hidden`) |
| الصفحات ذات تكيّف جزئي | 120 صفحة من 139 تستعمل `md:` بدرجات متفاوتة |
| لوحة التحكم / POS / المتجر | شبكات `grid-cols` متجاوبة جزئياً |
| الجداول (سجل المبيعات، التقارير…) | ⚠️ `<table>` بلا بديل — تعتمد على `overflow-x-auto` (تمرير أفقي متعب على الجوال) |
| سجل المبيعات SalesHistoryPage | ⚠️ صفر كلاسات تجاوب |
| النوافذ المنبثقة (Dialogs) | معظمها `max-w-md/lg` — مقبولة لكن تحتاج ضبط padding على الشاشات الصغيرة |
| الأداء على الجوال | ✅ حُلّ في p273: الحزمة الابتدائية 452KB بدل 5.4MB (تحميل كسول لكل صفحة) |

## 2. نقاط الكسر المعتمدة

| المقاس | النطاق | السلوك |
|---|---|---|
| جوال | `< 768px` | تنقّل سفلي (Bottom Bar) + درج جانبي + جداول→بطاقات |
| تابلت | `768–1024px` | درج جانبي مصغّر (أيقونات) + شبكات عمودين + جداول مبسّطة |
| سطح مكتب | `> 1024px` | **بدون أي تغيير — الشكل الحالي كما هو** |

## 3. خطة التنفيذ المرحلية (بعد الموافقة فقط)

### المرحلة أ — التنقل السفلي للجوال (أولوية قصوى)
- شريط سفلي ثابت بخمسة اختصارات قابلة للتهيئة: الرئيسية، POS، المبيعات، المنتجات، «المزيد» (يفتح الدرج الكامل).
- لا يظهر إلا `< md`؛ الترويسة والدرج الحاليان يبقيان كما هما.
- الاختصارات تقرأ صلاحيات المستخدم (cashier يرى POS والمبيعات فقط مثلاً).

### المرحلة ب — نمط «الجدول → بطاقات» (مكوّن موحّد)
- مكوّن `ResponsiveTable` جديد: يستقبل نفس بيانات الجدول ويعرض `<table>` على `md+` وبطاقات مكدّسة (أهم 3-4 حقول + توسيع) على الجوال.
- يُطبَّق تدريجياً على الصفحات الأكثر استعمالاً أولاً:
  1. سجل المبيعات SalesHistoryPage (صفر تجاوب حالياً)
  2. المنتجات ProductsPage
  3. العملاء / الموردون
  4. التقارير اليومية وصندوق طلبات ecom

### المرحلة ج — POS بمسارات لمسية (جوال/تابلت)
- أزرار منتجات أكبر (min 48×48px — معيار اللمس)، شبكة 2-3 أعمدة على الجوال.
- سلة منزلقة من الأسفل (bottom sheet) بدل العمود الجانبي على الشاشات الضيقة.
- تثبيت زر الدفع أسفل الشاشة.

### المرحلة د — تابلت: استغلال العرض
- قوائم + تفاصيل جنباً إلى جنب (master-detail) في: المنتجات، المبيعات، الوارد الاجتماعي، تذاكر الدعم.
- الدرج الجانبي المصغّر (أيقونات فقط) كافتراضي على `md` مع إمكانية التوسيع.

### المرحلة هـ — اللمسات الأخيرة
- مراجعة كل Dialog: padding أصغر + أزرار بعرض كامل على الجوال.
- حقول الإدخال `input[type=number]` و`inputmode` مناسبة للوحات الجوال.
- منع التكبير التلقائي في iOS عند التركيز (font-size ≥ 16px في الحقول).
- اختبار RTL على الشاشات الصغيرة (اتجاه الدرج والشرائط).

## 4. ما لن يتغيّر أبداً
- الألوان والخطوط والتخطيط على سطح المكتب.
- أي مسار API أو منطق خلفي — الوثيقة واجهة فقط.
- بيانات أي مستأجر.

## 5. معايير القبول لكل مرحلة
1. لقطة شاشة جوال (375px) وتابلت (768px) وسطح مكتب (1440px) لكل صفحة معدّلة — **سطح المكتب يجب أن يبقى مطابقاً بكسلياً**.
2. كل عنصر قابل للنقر ≥ 44×44px على الجوال.
3. لا تمرير أفقي في أي صفحة على 375px.
4. اختبار حقيقي عبر متصفح headless على العرضين + اختبار يدوي على هاتف المالك.
5. كل مرحلة تُنشر وتُختبر وتُوثّق وتُسلَّم بـ commit مستقل (مثل باقي المراحل).

## 6. تقدير الحجم
| المرحلة | الصفحات المتأثرة | الحجم التقريبي |
|---|---|---|
| أ — التنقل السفلي | Layout واحد | صغيرة |
| ب — الجداول→بطاقات | ~12 صفحة رئيسية أولاً | متوسطة |
| ج — POS | صفحة واحدة معقّدة | متوسطة |
| د — تابلت master-detail | 4-6 صفحات | متوسطة |
| هـ — اللمسات | عشرات الحقول/الحوارات | صغيرة-متوسطة |

---

**بانتظار موافقتك**: أي مرحلة تبدأ بها؟ (التوصية: أ ثم ب — أثر فوري على الاستعمال اليومي من الهاتف)
